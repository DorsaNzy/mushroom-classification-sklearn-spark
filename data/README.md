# Data

The raw dataset is not included in this repository.

To reproduce the notebook, place the Mushroom dataset locally at:

```text
data/agaricus-lepiota.data
```

The notebook expects this file path by default. If your file is stored elsewhere, update the path in the notebook.
